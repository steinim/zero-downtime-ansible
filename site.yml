---
- hosts: dbservers
  gather_facts: False
  roles:
    - etc_hosts
    - common
    - postgresql
    - db

- hosts: appservers
  serial: 1
  roles:
    - etc_hosts
    - common
    - java
    - users
    - app

- hosts: proxies
  gather_facts: False
  roles:
    - etc_hosts
    - common

