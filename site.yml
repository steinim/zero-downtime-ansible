---

- hosts: dbservers
  gather_facts: False
  roles:
    - etc_hosts
    - common
    - postgresql
    - db

- hosts: appservers
  serial: 1
  roles:
    - etc_hosts
    - common
    - users
    - app

- hosts: proxies
  gather_facts: False
  vars:
    - app_port: "1234"
  roles:
    - etc_hosts
    - common
    - nginx

